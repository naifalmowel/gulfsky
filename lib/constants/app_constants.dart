class AppConstants {
  // Responsive Breakpoints
  static const double mobileBreakpoint = 600.0;
  static const double tabletBreakpoint = 768.0;
  static const double desktopBreakpoint = 1200.0;
  static const double largeDesktopBreakpoint = 1400.0;

  // Animation Durations (in milliseconds)
  static const int shortAnimationDuration = 300;
  static const int mediumAnimationDuration = 800;
  static const int longAnimationDuration = 1200;
  static const int extraLongAnimationDuration = 2000;

  // Spacing
  static const double smallSpacing = 10.0;
  static const double mediumSpacing = 20.0;
  static const double largeSpacing = 40.0;
  static const double extraLargeSpacing = 60.0;

  // Padding
  static const double mobilePadding = 20.0;
  static const double tabletPadding = 40.0;
  static const double desktopPadding = 80.0;

  // Border Radius
  static const double smallRadius = 10.0;
  static const double mediumRadius = 15.0;
  static const double largeRadius = 20.0;
  static const double extraLargeRadius = 30.0;

  // Font Sizes
  static const double h1FontSize = 48.0;
  static const double h2FontSize = 36.0;
  static const double h3FontSize = 28.0;
  static const double h4FontSize = 20.0;
  static const double bodyFontSize = 16.0;
  static const double smallFontSize = 14.0;

  // Grid Aspect Ratios (for responsive grids)
  static const double largeDesktopAspectRatio = 0.0005;
  static const double desktopAspectRatio = 0.00071;
  static const double tabletAspectRatio = 0.0012;
  static const double mobileAspectRatio = 0.00285;

  // Hero Section Heights
  static const double desktopHeroHeight = 900.0;
  static const double mobileHeroHeight = 1100.0;

  // Navigation
  static const double navbarHeight = 80.0;
  static const double logoSize = 50.0;

  // Delays (in milliseconds)
  static const int scrollInitDelay = 100;
  static const int routeNavigationDelay = 300;

  // Scroll Settings
  static const double scrollAlignmentTop = 0.0;
  static const double visibilityThreshold = 0.3;
}
